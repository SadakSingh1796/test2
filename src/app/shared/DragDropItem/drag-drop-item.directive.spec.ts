import { DragDropItemDirective } from './drag-drop-item.directive';

describe('DragDropItemDirective', () => {
  it('should create an instance', () => {
    const directive = new DragDropItemDirective();
    expect(directive).toBeTruthy();
  });
});
