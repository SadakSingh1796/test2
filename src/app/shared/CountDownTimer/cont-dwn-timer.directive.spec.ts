import { ContDwnTimerDirective } from './cont-dwn-timer.directive';

describe('ContDwnTimerDirective', () => {
  it('should create an instance', () => {
    const directive = new ContDwnTimerDirective();
    expect(directive).toBeTruthy();
  });
});
